version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: secopstodo
    ports:
      - "8081:5000"   # VM port 8081 → container port 5000
    environment:
      - NODE_ENV=production
